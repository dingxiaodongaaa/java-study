## 关键字

`poublic` `void` `class` `static` 等等

## 标识符

指在程序中，我们自己定义内容。比如类的名字，方法的名字和变量的名字等等，都是标识符。

### 命名规则（硬性要求）

- 标识符可以包含数字、字母、下划线、美元符号
- 标识符不能以数字开头
- 标识符不能是关键字

### 命名规则（软性要求）

- 类名规则：首字母大写，后面每个单词首字母大写（大驼峰）
- 方法名规范：首字母小写，后面每个单词首字母大写（小驼峰）
- 变量名规范：全部小写
